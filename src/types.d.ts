export type Option = {
  value: string;
  label: string;
};

export interface FormData {
  category: string;
  service: string;
  squareMeters: number;
  // Define other fields as needed based on your steps
}
